class Mark {

    static iconteste = L.icon ({
        iconUrl: "Coffee.png"
      })

        constructor(map, data) {
            this.map = map;
            this.data = data;
        }

    static markMap(element, map) {

    

        // console.log(e.lat, e.lng, map)

        // L.marker([e.lat, e.lng]).addTo(map)

        // console.log(obj)

        // for(let e of obj) {
            // console.log(e.lat, e.lng, e.message, e.map)
            // L.marker([e.lat, e.lng])
            // .bindPopup(e.message)
            // .add(e.map)

            //    L.marker([e.lat, e.lng])
            // {
            //   iconSize: [100, 100]
            //   // icon: iconteste
            // })
            // .addTo(map);

            // let aux = e

            // console.log(aux.lat)

            /* for(let e of aux) {
                console.log(e)
            } */
        // }

        // const obj = {
        //     map: map,
        //     lat: lat,
        //     long: long,
        //     message: message
        // }

        // console.log(obj)

    //   console.log(lat)
    //   console.log(long)
    //   console.log(message)

    //  console.log(map)

    // console.log(marks)


        // var locations = [
        //     ["LOCAL1", -29.96136, -51.62225],
        //     ["LOCAL2", -29.96383, -51.63643]
        //   ]

         
        //    L.marker([lat, long])
            // {
            //   iconSize: [100, 100]
            //   // icon: iconteste
            // })
            // .bindPopup(message)
            // .addTo(map);
          
    }
}