/* const routes = [
    "ratos",
    "charqueadas",
    "são jeronimo"
]; */

/* window.localStorage.setItem("routes", JSON.stringify(routes)) */

/* const rotas = JSON.parse(localStorage.getItem("rota")); */

/* rotas != null ? console.log(true) : console.log(false) */
/* 
rotas.forEach(e => {
    console.log(e)
}); */

/* var rota = new Rota(
    [
        "https://www.strava.com/api/v3/routes/3007667420057044860?access_token="
        ],
        "map"
); */

/* console.log(rota.reAuthorize()) */

let cores = ["azul", "amarelo", "vermelho"]

for(let cor of cores) {
    console.log(cor)
}